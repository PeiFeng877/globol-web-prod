# src/lib/GEMINI.md - 工具与解析 (L2)
[PROTOCOL]: 变更时更新此头部，然后检查 GEMINI.md

## 1. 🗺 地图 (结构)
```
lib/
├── ai/
│   ├── GEMINI.md
│   ├── chat-prompt.ts
│   └── gateway.ts
├── constants.ts
├── content.ts
└── firebase/
    ├── client.ts
    └── analytics.ts
```

## 2. 📄 文件说明 (Files)
- `constants.ts`: 环境变量驱动的全局常量（如 BASE_URL）。
- `content.ts`: Markdown 读取与 HTML 渲染，输出文章数据模型与法律文本内容。
- `ai/chat-prompt.ts`: 角色人设与上下文 Prompt 组装器。
- `ai/gateway.ts`: Vercel AI Gateway 调用封装。
- `firebase/client.ts`: Firebase 客户端初始化与 Analytics 接入入口。
- `firebase/analytics.ts`: Analytics 事件上报封装与埋点入口。

## 3. 🔗 依赖边界 (Dependencies)
- `lib/` 只依赖第三方库与 Node API，不依赖页面组件。

## 4. 📐 规范 (Rules)
- 逻辑保持纯函数风格，避免与 UI 耦合。
- 修改输出结构必须同步更新 `app/` 与 `components/` 使用方。

## 5. 🧾 变更日志 (Changelog)
- 2026-02-08: 新增 `ai/` 子模块，提供聊天 Prompt 组装与 Gateway 调用封装。
- 2026-02-03: 建立 lib 模块 L2 文档与文件清单。
- 2026-02-05: `content.ts` 扩展为支持法律文本解析。
- 2026-02-05: 新增 Firebase 客户端初始化入口。
- 2026-02-06: 新增 Firebase Analytics 事件上报封装。
